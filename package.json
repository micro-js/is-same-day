{
  "name": "@f/is-same-day",
  "description": "Check if two dates are on the same day",
  "repository": "git://github.com/micro-js/is-same-day.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
